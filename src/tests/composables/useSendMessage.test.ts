import { describe, it } from 'vitest';
import { useSendMessage } from '@/composables/useSendMessage';

describe('useSendMessage composable', () => {
  it('should send a message and return the correct response', async () => {});
});
